input Pagination {
  first: Int = 10
  skip: Int = 0
}

type PaginationOutput {
  first: Int
  skip: Int
}

type PaginationInfos {
  total: Int
  first: Int
  skip: Int
}

enum SortDirection {
  asc
  desc
}
